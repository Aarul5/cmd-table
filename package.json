{
  "name": "cmd-table-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "cmd-table monorepo workspace root",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "changeset": "changeset"
  },
  "devDependencies": {},
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
