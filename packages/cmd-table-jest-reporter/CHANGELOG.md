# Changelog

## [0.1.1] - 2026-04-30

### Fixed

- Moved `cmd-table` from `peerDependencies` to `dependencies` so it installs automatically. Users now only need `npm install cmd-table-jest-reporter`.

## [0.1.0] - 2026-04-30

### Initial Release

- Jest custom reporter rendering suite-level summaries, per-test breakdowns, and slowest-tests leaderboard as formatted ASCII tables via cmd-table.
